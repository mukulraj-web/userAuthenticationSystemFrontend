### Phase-1 :-
building the basic architech for connection to backend name -- <b>userAuthenticationSystem <b>
In this we have made simple register form using sort of react fundamental useState to set the form data that will be send to the backend.
we have made 2 more function like handleChange and handleSubmit to handle the form safely to the form state and submitting the data from the user to backend via the fromData and setFormData using preventDefault function to prevent all default settings of react and axios to safely send the data the ## REGISTER route.
lastly we made the change in <h1>vite.config.js<h1> file to adding proxy for <b> CORS <b> 
### Conclusion :- Successfully registered first user the app.